{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capability for the main desktop dashboard window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
