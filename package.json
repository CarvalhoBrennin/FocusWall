{
  "type": "module",
  "name": "focus-desktop-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop dashboard em Tauri com Svelte e Vite.",
  "scripts": {
    "check:setup": "node tools/check-setup.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build --no-bundle",
    "tauri:build:installer": "tauri build",
    "open": "abrir-dashboard.bat",
    "open:dev": "npm run tauri:dev",
    "dist": "npm run tauri:build && build-bootstrapper.bat"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tauri-apps/cli": "~2.10.1",
    "typescript": "^5.9.2",
    "vite": "^8.0.14",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@tauri-apps/api": "~2.10.1",
    "@tauri-apps/plugin-process": "~2.3.0",
    "@tauri-apps/plugin-updater": "~2.10.1",
    "svelte": "^5.55.9"
  }
}
