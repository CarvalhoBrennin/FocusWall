/** Set to true to expose the OpenCode panel tab (add tab in TaskHeader.svelte; uncomment blocks in TaskPanel.svelte). */
export const OPENCODE_TAB_ENABLED = false;

/** Set to true to expose the Vivarium panel (uncomment blocks in TaskPanel.svelte). */
export const VIVARIUM_ENABLED = false;
