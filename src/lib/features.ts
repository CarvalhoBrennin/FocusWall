/** Set to true to expose the OpenCode panel tab (also uncomment UI in TaskHeader.svelte / TaskPanel.svelte). */
export const OPENCODE_TAB_ENABLED = false;

/** Set to true to expose the Vivarium panel tab and lazy-load its UI. */
export const VIVARIUM_ENABLED = false;
